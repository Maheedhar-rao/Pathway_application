flask==3.0.0
flask-wtf>=1.2.0
gunicorn>=21.0.0
psycopg[binary]>=3.1
python-dotenv
supabase
reportlab>=4.0.0
cryptography>=42.0.0
# IANA tz database as a Python package, so zoneinfo.ZoneInfo("America/New_York")
# resolves on slim containers (Railway) that ship no system tz database. Without
# it the "Submitted … ET" stamps silently fall back and misreport the time.
tzdata

# NAICS/SIC classification + Census crosswalk. Canonical home for the keyword
# rules -- edit them there, not here.
#
# Tarball, NOT git+https: pip shells out to the git binary for a git+ URL and
# the Railway build image has none, so a git+ pin fails the build. The
# /archive/refs/tags/<tag>.tar.gz form installs over plain HTTPS and pins the
# exact released ruleset.
pathway-naics @ https://github.com/Maheedhar-rao/pathway-naics/archive/refs/tags/v0.2.0.tar.gz
